```
H, M = map(int,input().split())
need = int(input())
clock = H * 60 + M + need

new_h = clock // 60
new_m = clock % 60

if new_h >= 24 :
    new_h -= 24

print(new_h, new_m)

```




